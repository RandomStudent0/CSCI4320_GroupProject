<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CSCI4320Proj"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add more pages here-->
        <activity android:name=".MainIOTPage"></activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".NewIOT"></activity>
        <activity android:name=".DeleteIOT"></activity>
        <activity android:name=".EditIOT"></activity>
        <activity android:name=".ListIOT"></activity>
        <activity android:name=".CameraMainActivity"></activity>
        <activity android:name=".Camera1Main"></activity>
        <activity android:name=".Camera1Play"></activity>
        <activity android:name=".Camera1Rec"></activity>
        <activity android:name=".Camera2Main"></activity>
        <activity android:name=".Camera2Play"></activity>
        <activity android:name=".Camera2Rec"></activity>
        <activity android:name=".RobotVacMain"></activity>

    </application>

</manifest>
